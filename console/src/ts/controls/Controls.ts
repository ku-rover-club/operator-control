import map from './map.json'

/**
 * Controls class that handles gamepads and function calls
 */
export class Controls {
    /**
     * Constructor
     * Hook into call the usefult Gamepad callbacks
     */
    constructor() {

    }
}
